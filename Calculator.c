#include <stdio.h>

int main() {
  int a,b;
  int sum;
  int diff;
  scanf("%d %d", &a, &b);
  sum = a + b ;
  diff = a - b ;
  printf("Sum is: %d \n", sum);
  printf("Difference is: %d", diff );
  return 0;
}
