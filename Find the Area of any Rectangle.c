#include <stdio.h>

int main() {
  int length, width;
  int area ;
  scanf("%d", &length);
  scanf("%d", &width);
  area = length * width;
  printf("Area of the rectangle is: %d", area);
  return 0;
}
